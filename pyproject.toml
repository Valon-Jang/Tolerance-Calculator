[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tolerance-calculator"
version = "0.1.0"
description = "Deterministic engineering tolerance stack-up analysis"
requires-python = ">=3.10"
authors = [{name = "Valon Jang"}]

[project.scripts]
tolerance-calculator = "tolerance_calculator.cli:main"
